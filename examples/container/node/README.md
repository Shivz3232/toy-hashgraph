# How to run

## Docker build

From the project root: `docker build -t hashgraph -f examples/container/Dockerfile .`

## Docker compose

From the project root: `docker compose -f examples/container/docker-compose.yml up`